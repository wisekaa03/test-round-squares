# RoundSquares

## Run tasks

To run the dev server for your app, use:

```sh
npx nx serve backend-api
```

To create a production bundle:

```sh
npx nx build backend-api
```

To see all available targets to run for a project, run:

```sh
npx nx show project backend-api
```

```sh
npx nx dev web-vite
```
